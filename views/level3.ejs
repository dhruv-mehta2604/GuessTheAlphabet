<html>

<head>
    <title>Level3</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
</head>

<% arr=helper.genTableThree()%>
    <%var ques=helper.genQuesFromTable(arr);%>

        <body>
            <h1>Level3</h1>
            <h2>
                <%=ques%>
            </h2>
            <h3>no of tries =
                <span id="noOfTries"><%=lthreer%></span>
            </h3>
            <h4 id="gameOver"></h4>
            <table class="table table-bordered">
                <tbody>
                    <%for (let i = 0; i < 4; i++) {%>
                        <tr>
                            <%for (let j = 0; j < 4; j++) {%>
                                <td>
                                    <%=arr[i][j]%>
                                </td>
                                <%}%>
                        </tr>
                        <%}%>
                </tbody>
            </table>
            <script>
                lthreer = 0;
                var ques = "<%=ques%>";
                const tbody = document.querySelector('tbody');
                const span = document.querySelector('#noOfTries');
                const gameOver = document.querySelector('#gameOver');
                tbody.addEventListener('click', function(e) {
                    const cell = e.target.closest('td');
                    if (!cell) {
                        return;
                    } // Quit, not clicked on a cell
                    const row = cell.parentElement;
                    lthreer += 1;
                    if (cell.innerText == ques[27]) {
                        cell.style.backgroundColor = "green";
                        if (lthreer == 1) {
                            gameOver.innerHTML = 'you won the game!</a>';
                        } else {
                            gameOver.innerHTML = 'You have to retry <a href="/level3">this level</a>';
                        }
                    } else {
                        cell.style.backgroundColor = "red";
                    }
                    console.log(lthreer);
                    span.innerText = lthreer;
                    console.log(cell.innerHTML, row.rowIndex, cell.cellIndex);
                });
            </script>
        </body>

</html>